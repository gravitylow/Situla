{% load url from future %}
{% if project_list %}
    <ul>
    {% for project in project_list %}
        <li><a href="{% url 'project' project.id %}">{{ project.project }}</a></li>
    {% endfor %}
    </ul>
{% elif project %}
	<strong>{{ project.project }}</strong> by {{ project.user }}.
        <br>
        Rating: <strong>{{ project.rating }}</strong>
{% else %}
    <p>No projects are available.</p>
{% endif %}

